{
  "name": "external",
  "version": "1.0.0",
  "description": "external app",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:integration": "docker-compose -f ../compose/docker-compose.yml -f ./docker-compose.yml up -d && docker wait test && exit $?"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/docker-multiple-app-test-example/external.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/docker-multiple-app-test-example/external/issues"
  },
  "homepage": "https://github.com/docker-multiple-app-test-example/external#readme",
  "dependencies": {
    "express": "^4.16.3",
    "request": "^2.85.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "jest": "^22.4.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
